# Benchmark authoring

A task has metadata in `benchmarks/manifest.json`, a generated visible challenge source, a hidden evaluator and a mutation operator. Keep hidden oracles out of the agent workspace. Prefer behavioral checks over text matching, deterministic fixtures and failure modes that represent real engineering work.
