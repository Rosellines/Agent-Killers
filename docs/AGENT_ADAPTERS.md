# Agent adapters

Edit `adapters/agents.json` when a provider's CLI syntax differs from the included template. External agent CLIs change over time; adapter success on your machine is the compatibility signal.
